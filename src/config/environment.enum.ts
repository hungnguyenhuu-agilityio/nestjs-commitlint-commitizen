/**
 * Define enum for environment
 * Currently, we support following environment:
 * + Local
 * + Staging
 * + Production
 */
export enum Environment {
  Local = 'local',
  Staging = 'staging',
  Production = 'production',
}
